services:
  app:
    build: .
    ports: 
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb://mongodb:27017
    depends_on:
      - mongodb 
      
  mongodb:
    image: mongo:latest
    ports:
      - "27018:27017"
    volumes:
      - movie_db:/data/db

volumes:
  movie_db: